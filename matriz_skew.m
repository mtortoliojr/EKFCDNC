%%--------------------------------------------------------------------------------
%% Função para o cálculo da matrix skew de r
%%--------------------------------------------------------------------------------
function rX = matriz_skew(r)

% Matriz [rX]
rX = [0 -r(3) r(2); 
	  r(3) 0 -r(1); 
	 -r(2) r(1) 0];

end
